apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "looking-glass.fullname" . }}-test-connection"
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "looking-glass.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "looking-glass.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
